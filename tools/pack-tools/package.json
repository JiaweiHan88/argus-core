{
  "name": "@argus/pack-tools",
  "version": "0.1.0",
  "description": "Generic packager for Argus packs — validate, assemble, checksum, zip. Never compiles binaries.",
  "bin": { "argus-pack": "dist/cli.js" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node build.mjs"
  },
  "dependencies": {
    "zip-lib": "^1.4.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
