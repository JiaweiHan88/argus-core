{
  "name": "@argus/distill-eval",
  "version": "0.1.0",
  "description": "Replay + judge harness for the case-distill prompt over an exported eval corpus. Dev tool; never ships in the app.",
  "bin": { "argus-distill-eval": "dist/cli.js" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@types/node": "^22",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
