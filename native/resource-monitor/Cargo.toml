[package]
name = "argus-resource-monitor"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.39"

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
strip = true
